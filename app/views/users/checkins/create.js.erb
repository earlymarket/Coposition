<%= render("/shared/notifications") %>
$('#checkin-table').prepend('<%= j render("users/devices/checkin_table_row", checkin: @checkin) %>');
checkins.push(<%= raw @checkin.to_json %>)
markers.clearLayers();
renderMarkers();
