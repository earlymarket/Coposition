if (utility.urlParam("redirect")) {
  window.location.replace(utility.urlParam("redirect"));
}
($("#<%= "app_#{@approval.id}" %>")).remove();
$('#approvedDevelopers').html("<%= j (render partial: 'approved_developers', locals: {apps: @apps}).delete("\n").html_safe %>")
$('#friends').html("<%= j (render partial: 'friends', locals: {friends: @friends}).delete("\n").html_safe %>")
$('#friendRequests').html("<%= j (render partial: "friend_requests", locals: {pending: @pending}).delete("\n").html_safe if @pending.present? %>")
$('#pendingApprovals').html("<%= j (render partial: 'pending_approvals', locals: {pending: @pending}).delete("\n").html_safe if @pending.present? %>")
