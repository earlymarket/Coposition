if (COPO.utility.urlParam("redirect")) {
  window.location.replace(COPO.utility.urlParam("redirect"));
}
($("#<%= "app_#{@approval.id}" %>")).remove();
$('#approvedDevelopers').html("<%= j (render partial: 'approved_developers', locals: {apps: @apps}).html_safe %>")
$('#friends').html("<%= j (render partial: 'friends', locals: {friends: @friends}).html_safe %>")
$('#friendRequests').html("<%= j (render partial: "friend_requests", locals: {pending: @pending}).html_safe if @pending.present? %>")
$('#pendingApprovals').html("<%= j (render partial: 'pending_approvals', locals: {pending: @pending}).html_safe if @pending.present? %>")
