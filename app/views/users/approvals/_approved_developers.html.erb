<!-- Use single quotes (''), so that this can be user in a JS template -->
<div class='list-group'>
  <% @approved_devs.each do |app| %>
    <div class='list-group-item'>
      <a href='<%= switch_all_privileges_for_developer_user_devices_path(current_user.url_id, developer: app.approvable_id) %>' data-method='post' data-remote='true' class='white-link'> 
        <h4 class='list-group-item-heading'><%= Developer.find(app.approvable_id).company_name %></h4>
        <% if Developer.find(app.approvable_id).tagline %>
          <p class='list-group-item-text'><%= Developer.find(app.approvable_id).tagline %></p>
        <% end %>
      </a>
      <div class='row pt-m'>
        <div class='col-lg-12 list-group-item-text'>
          <% app.user.devices.each do |device|  %>
            <a href='<%= switch_privilege_for_developer_user_device_path(current_user.url_id, device.id, developer: app.approvable_id) %>' data-method='post' data-remote='true' class='white-link' >
              <div id='<%= "device#{device.id}developer#{app.approvable_id}" %>' class='col-lg-3 dev-box small-curved-border small-height clearfix <%= "privilege-#{device.privilege_for(Developer.find(app.approvable_id))}" %>'>
                <%= device.name %>
              </div>
            </a>
          <% end %>
        </div>
      </div>
    </div>
  <% end %>
</div>