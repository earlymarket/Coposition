<div id='gonvariables'>
  <%= Gon::Base.render_data %>
</div>
<div id='device-name'class="valign-wrapper">
  <i class="material-icons medium">
    <%= @presenter.device.icon %>
  </i>
  &nbsp;
  <%= @presenter.device.name %>
</div>

<div class="myProgress">
  <div class="myDeterminate" style="width: 0%"></div>
</div>

<ul id='filters' class='date-select collapsible'>
  <li>
    <div class='collapsible-header' id='date-range-toggle'>Select a date range</div>
    <div class='collapsible-body'>
      <% friend = @presenter.respond_to? :friend %>
      <%= form_for @presenter.form_for, url: @presenter.form_path, class: "form-group", method: :get do |f| %>
        <%= f.label 'From' %>
        <%= text_field_tag(:from, humanize_date(@presenter.date_range[:from]), class: "datepick", id: "input_from", required: true) %>
        <%= f.label 'to' %>
        <%= text_field_tag(:to, humanize_date(@presenter.date_range[:to]), class: "datepick", id: "input_to", required: true) %>
        <%= f.label 'Or' %>
        <div class='date-filter-presets'>
          <%= @presenter.form_range_filter('LAST 24H', 1.day.ago) %>
          <%= @presenter.form_range_filter('LAST WEEK', 1.week.ago) %>
          <%= @presenter.form_range_filter('LAST MONTH', 1.month.ago) %>
          <%= @presenter.form_range_filter('LAST YEAR', 1.year.ago) %>
        </div>
        <%= f.submit("Apply", class: "form-control btn btn-default") %>
        <%= link_to("Clear", @presenter.form_path, { method: :get, class: "btn-flat right" })%>
        <%= hidden_field_tag(:device_id, @presenter.device.id ) %>
      <% end %>
    </div>
  </li>
</ul>

<div id='map'></div>
