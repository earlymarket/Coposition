  <hr>
  <%= link_to(@fogmessage, fog_user_device_path(current_user.id, @device.id), { method: :put, class: "btn btn-info btn-lg m-m", id:"fogButton", remote: true, data: { fogged: @device.fogged? }  })%>

      Timeshift with a delay of 
      <%= form_tag(set_delay_user_device_path(current_user.id, @device.id) + ".js" , method: "post", class:"inline", remote: true) do %>
        <%= text_field_tag(:mins, @device.delayed, size: 2) %>
      minutes.
        <%= submit_tag("Update", class: "btn btn-default btn-xs btn-primary") %>
      <% end %> 

  <hr>