Downloaded checkins
<% if activity.trackable %>
  <br/>
    Device name: <%= link_to(activity.trackable.name , activities_path(filter: { trackable_type: "Device", trackable_id: activity.trackable.id })) %>
<% else %>
  , device has since been destroyed.
<% end %>
<br/>
  Params: <%= activity.parameters %>

